# Film clips this machine expects, one per line.
#
# The name is what widget_player passes as the `clip` token: no folder, no
# extension. Blank lines and lines starting with # are ignored.
#
# The clips themselves are not in version control, so this file is how a fresh
# checkout knows what is missing. Check it with:
#
#     python3 tools/check_video.py
#
# Sample clip, tracked in the repo. Copy it in with:
#   cp gmc/slides/attract/assets/matrixrain.ogv gmc/video/
matrixrain

# Act I clips. Referenced by the mode configs (see docs/11-rules-act-1.md,
# section 10) and not yet cut, so check_video.py reports them missing until
# they are. Each widget_player entry has an `expire`, so a missing clip leaves
# the stage after that time rather than blocking it.
trinity_room_303
phone_booth_truck
pills_choice
pod_wake
kung_fu_load
jump_program
deja_vu_cat
lots_of_guns
lobby_shootout
helicopter_catch
trinity_mb_intro
sentinel_attack
subway_smith
neo_resurrects
emp_fire
